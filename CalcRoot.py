def calc_root(radicand, index): # Function Definition
    i = 1 / index # Calculates the number by which the radicand has to be raised (index / 1)
    root = radicand ** i # Raise the radicand by that number
    return root # Returns the root value